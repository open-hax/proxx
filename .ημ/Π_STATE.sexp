(pi-handoff
  (timestamp "20260529T040325Z")
  (kind eta-mu-kanban-migration)
  (manifest "/tmp/eta-mu-kanban-batches/agent_inbox.json")
  (verification "migrate-specs-to-kanban.mjs plus eta-mu-beta kanban count for each board")
  (concurrency "path-scoped staging only; unrelated dirt left untouched"))
