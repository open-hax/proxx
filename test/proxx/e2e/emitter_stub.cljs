;; This file is intentionally empty — the real emitter is now at
;; src/proxx/ledger/emitter.cljs and shadow-cljs will resolve it first.
;; Kept as a placeholder so git history shows the red→green transition.
